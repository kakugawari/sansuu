# さんすうドリル

小学生・中学生 向けの 算数/ 数学の 練習アプリ。**面積・体積・食塩水の濃度・割合・
速さ・平均・分数・方程式** の 8 ジャンル × 3 レベルを、その場で 作って 出す。
問題は 作りおきでは なく、遊ぶ たびに 新しく 作られる (だから 覚えても 意味が ない)。

ビルドなし。素の HTML / CSS / JS だけで 動く。

---

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # 出題と採点のテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 playwright)
npm run icon       # ホーム画面用の icon-180.png を 作りなおす
npm run artifact   # 全部を 1 枚に まとめた artifact.html を 作る (テストプレイ用)
```

`npm run test:ui` は 本物の ブラウザを 立ち上げ、指の操作 (タップ) を そのまま 再現する。
**画面を 触ったときの 不具合は こちらでしか 捕まらない。変更したら 必ず 両方 流す。**

## 構成

```
index.html / styles.css   画面
core.js                   出題と採点。DOM を触らない。ブラウザと node の両方で動く
app.js                    操作・描画・図(SVG)・テンキー・記録
core.test.js              出題と採点のテスト (node --test)
browser-test.js           ブラウザで動かすテスト
serve.js                  開発用サーバー
make-icon.js              icon-180.png を作る (zlib だけで PNG を書く)
build-artifact.js         全部を 1 枚の HTML に まとめる
```

ファイルは すべて 直下に 置く。スマホや タブレットからでも 1 ファイルずつ
差し替えられる ように するため。

`window.__app` は テストから 中身を のぞく ための 入口 (`browser-test.js` が 使う)。

---

## 決めごと

### 問題は「答えから 逆算して」作る

いちばん 大事な ところ。**あとで 検算するのでは なく、そもそも 割り切れない 問題が
生まれない 作りに する。**

- 食塩水: 先に 濃度 `p%` を 決め、食塩の量が 整数に なる 重さ (100/gcd(p,100) の 倍数)
  からしか 全体量を 選ばない
- 混ぜる 問題: 先に 「混ぜた あとの 濃度 p3」と 比 `m:n` を 決め、
  `p1 = p3 + n×t`, `p2 = p3 − m×t` と 逆算する。こうすると 混合後は 必ず p3 に なる
- 三角形・台形: 面積が 整数に なるよう、`底辺 × 高さ` が 奇数なら 高さを 1 増やす
- 平均: 差を `+d` と `−d` の 組で 作る。合計が ずれず、0 点や 120 点も 出ない
- 追いつき算・往復の平均の速さ: 答えが 整数に なる 組だけを 表に 持つ
- 分数: 問題文に 出す 分数は 約分ずみの 形だけ (`3/9` を 出さない)。通分は 最小公倍数で

`core.test.js` が 8 ジャンル × 3 レベル × 400 seed = **9600 問**を 毎回 作り、
「答えが 整数か 決めた 桁の 小数か 約分ずみの 分数に なっているか」
「テンキーだけで 打てる 形か」「問題文に NaN や undefined が 出ていないか」を 見張る。
さらに 食塩水と 面積は **問題文に 書かれた 数字だけを 正規表現で 抜き出して 検算** する
(問題文と 答えが ずれる 事故を これで 止める)。

### 画面

- **ロジックは `core.js`、画面は `app.js`。** core.js は DOM を 触らない ので node で テストできる
- 答えは **自前の テンキー** で 入れる。スマホの キーボードを 出さない (画面が 半分 隠れる)
- 問題の画面は **上だけ スクロール、テンキーは 下に すわったまま**。
  問題文が 長くても 図が 大きくても 「こたえる」が 画面の外へ 出ない
- 答え合わせの あとは テンキーを しまい、その場所に とき方を 出す
- 図は cm を そのまま 座標に して 組み立て、最後に まとめて 拡大する。
  だから 「たて 2cm よこ 9cm」は ちゃんと 細長く 見える
- 記録の 保存は **1 セットの おわりに 1 回だけ**。1 問ごとに 書くと 操作が つまる

### 答え方

- 分数の 答えは **約分していないと 正解に しない**。ただし 値が 合って いれば
  「おしい! 約分 できるよ」と 出して、×には せず やり直させる
- 分数の 問題に 小数で 答えたら 「分数で 答えてね」。これも やり直し
- 全角の 数字 (`１２３`)、`%` つき、`３／４` でも 読む

---

## 踏んだ 落とし穴 (同じ轍を 踏まない ために)

画面まわりは、動いている ように 見えて 壊れている ことが ある。実際に 踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 消した はずの ホーム画面が 問題の 上に 出たまま | `.screen { display: flex }` が、ブラウザ既定の `[hidden] { display: none }` を 上書きしていた | `[hidden] { display: none !important; }` を 書く。テストは 属性ではなく **高さが 0 か** を 見る |
| 「こたえる」が 画面の 外に あって 押せない | 全部を 1 本の スクロールに 入れていた | 上 (問題) だけ スクロール、下 (テンキー) は 固定に 分ける |
| 三角柱が 何の形か 分からない | 奥行きの ずらし幅を 実寸の 半分に していて、手前の 面より 大きく なっていた | ずらし幅に 上限を つける (手前の 辺の 6 割まで) |
| 問題文に `3/9` のような 約分できる 分数が 出る | 分子を 1〜分母−1 から 適当に 選んでいた | 分母と 互いに素な 分子だけ 選ぶ |
| 平均の 問題に 120 点の 人が 出る | 最後の 1 人で 合計を つじつま合わせ していた | 差を `+d`/`−d` の 組で 作る |
| 置いた直後に 要素が 一瞬とぶ | CSS アニメーションの `transform: scale` が、位置決めの インライン `transform: translate` を 上書き | 位置は `left`/`top` で 決める |
| アニメーションが 1 フレームも 動かない | 同じ フレーム内で 書き換えると、ブラウザは 途中の 状態を 見ない | `element.animate()` (Web Animations API) を 使う |
| ホーム画面の アイコンが 違うものに なる | iOS は `apple-touch-icon` に SVG を 使えない | PNG を 用意する (`npm run icon`) |
| 直したのに 古い画面が 出る | サービスワーカーが キャッシュ優先だった | つながって いれば 最新を 取りにいく |
| 操作の たびに 一瞬止まる | 操作ごとに `localStorage` へ 同期で 書き込んでいた | 書き込みは まとめる |
| タップしても 反応しない | 上に 重ねた 層が 全面を ふさいでいた | 反応させたい 所だけ `pointer-events` を 有効にする |

**新しく 踏んだら、この表に 足すこと。**

---

## これから 足すなら

- 食塩水や 水そうの 図 (ビーカーの 絵に 濃度と 量を 書く)
- サービスワーカー (オフラインで 開ける ように する)。`browser-test.js` に
  「1 回の リロードで 直したものが 出るか」の テストが すでに 用意して ある
- 間違えた 問題を ためて、次の セットで 出しなおす
- 「くわしい とき方」を 図つきで 出す (面積の 分割など)

## 作り方 (毎回 これを なぞる)

1. **正しさを 仕組みで 保証できないか、先に 考える。** 「あとで 検算する」より
   「そもそも 間違いようが ない 作り」の 方が 強い
2. **「テストが 通った」で 終わらせない。実際に 動かして 目で 見る。**
   このアプリの 不具合も、テストは 全部 緑の まま 画面だけ 壊れて いた
3. 直したら **見張りの テストを 足す**。同じ 失敗を 二度 しない ための 唯一の 方法
4. スマホの 幅で 見る。指で 押す ところは 44px 以上
5. `npm run artifact` で 1 枚に まとめ、Artifact として 公開して リンクを 渡す。
   **その リンクは Claude 側の 仮ページ**で、本番の アドレスとは 別だと 必ず 伝える。
   直したら 同じ リンクを 更新する (新しい リンクを 作らない)
6. **作業用ブランチで 進める。main へは 言われてから 出す。**
   push の 前に `npm test` と `npm run test:ui` を 両方 流す
7. 報告は 事実で。**計測値や 試した 件数を 添える。**
   できて いないこと、飛ばした ことは そのまま 言う
